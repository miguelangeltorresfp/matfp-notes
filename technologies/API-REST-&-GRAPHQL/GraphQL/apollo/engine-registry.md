# APOLLO ENGINE GRAPHQL REGISTRY

* publish - `npx apollo service:push --endpoint=http://localhost:4000`

* check for any breaking changes - `npx apollo service:check --endpoint=http://localhost:4000`